* Docker creates it's isolated network where the containers are running in. So when two docker conatiners are deployed in this network, they get connected.

* By default, docker already provides some networks.

* 
